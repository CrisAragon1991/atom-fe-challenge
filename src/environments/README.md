// Instrucciones para usar variables de entorno en Angular
//
// 1. Usa `import { environment } from 'src/environments/environment';` en tus servicios o componentes.
// 2. Accede a las variables como: environment.apiUrl
// 3. Angular reemplaza automáticamente `environment.ts` por `environment.prod.ts` en builds de producción.
//
// Puedes agregar más variables en ambos archivos según tus necesidades.
